version: "3.9"

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./database.db:/app/database.db
      - ./static/qrs:/app/static/qrs
    env_file:
      - .env
    restart: unless-stopped